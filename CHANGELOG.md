# Changelog

All notable changes to the research software are recorded here.

## Unreleased

- package the reference implementation under `src/dela_sne`;
- add MIT licensing and software citation metadata;
- harden LAC convergence, restart, empty-cluster, and objective accounting;
- add PCA initialization, adaptive gains, automatic learning rate, and gradient stopping to exact t-SNE;
- make the paper bibliography derive from one canonical BibTeX source;
- replace byte-level figure reproducibility checks with pinned dependencies and numerical validation;
- make compiled manuscript PDFs CI/release artifacts instead of tracked build products;
- expand numerical, determinism, gradient, workflow, lint, type, and coverage checks.
