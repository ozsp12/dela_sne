# Data

Store only small, redistributable, or reproducibly generated datasets in this directory.

For external datasets, document the source, version, license, retrieval date, preprocessing steps, and a stable identifier or download location rather than committing large raw files without provenance.
